import {PropEmptyPlaceholderPipe} from './prop-empty-placeholder.pipe';

describe('PropEmptyPlaceholderPipe', () => {
  it('create an instance', () => {
    const pipe = new PropEmptyPlaceholderPipe();
    expect(pipe).toBeTruthy();
  });
});
